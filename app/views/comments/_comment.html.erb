<p>
  <%= comment.content %>
  <%= link_to 'Edit comment', edit_comment_path(id: comment.id) %>
  <%= link_to 'Show comment', comment_path(id: comment.id) %>
  <%= link_to 'Delete comment', comment_path(id: comment.id), method: :delete %>
</p>
