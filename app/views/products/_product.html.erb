<p>
  <%= product.name %>
  <%= link_to 'Edit product', edit_product_path(id: product.id) %>
  <%= link_to 'Show product', product_path(id: product.id) %>
  <%= link_to 'Delete product', product_path(id: product.id), method: :delete %>
</p>
